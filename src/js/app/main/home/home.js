(function () {
    'use strict';

    angular
        .module('home')
        .controller('home', init);

    init.$inject = [];

    function init() {
        var vm = this;
    }
}());
